import 'package:flutter/material.dart';

class ImageViewDetails extends StatelessWidget {
  const ImageViewDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}